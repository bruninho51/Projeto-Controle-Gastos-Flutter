# Implementation Plan: Refactor — InvestimentosPage para SharedAppBar

**Track ID:** investimentos-appbar_20260603
**Spec:** [spec.md](./spec.md)
**Created:** 2026-06-03
**Status:** [x] Complete

## Overview

Migrar a `InvestimentosPage` do `OrcamentosAppBar` legado para o
`SharedAppBar` padronizado, removendo o método `_buildAppBar()` e toda
a lógica manual de avatar. Em seguida, deletar o arquivo
`orcamentos_appbar.dart`, que ficará sem uso.

## Phase 1: Migrar InvestimentosPage para SharedAppBar

Substituir a AppBar legada pelo componente padronizado, adaptando
título, ícone, cores de gradiente e botão de refresh.

### Tasks

- [x] Task 1.1: Adicionar import de `shared_appbar.dart` e remover import de `orcamentos_appbar.dart`
- [x] Task 1.2: Remover o método `_buildAppBar()` por completo
- [x] Task 1.3: Definir constante `_gradientColors` com as cores do tema de investimentos (ex: verde/teal)
- [x] Task 1.4: Adicionar `SharedAppBar` diretamente no `appBar:` do `Scaffold`, com `title`, `subtitle`, `mainIcon`, `gradientColors` e `actionButtons` contendo o botão de refresh via `SharedAppBar.headerButton(...)`
- [x] Task 1.5: Remover import de `flutter/foundation.dart` (kIsWeb) se não for mais usado em nenhum outro lugar da page

### Verification

- [x] `flutter analyze` — zero warnings/errors
- [ ] Tela abre normalmente, AppBar exibe título e botão de refresh
- [ ] Avatar do usuário aparece corretamente (via `showAvatar: true`)
- [ ] Botão de refresh recarrega a lista de investimentos

## Phase 2: Remover OrcamentosAppBar

Com a migração concluída, o arquivo legado pode ser deletado com segurança.

### Tasks

- [x] Task 2.1: Deletar `lib/components/common/orcamentos_appbar.dart`
- [x] Task 2.2: Verificar se o diretório `lib/components/common/` ficou vazio e, se sim, deletá-lo também

### Verification

- [x] `flutter analyze` — zero warnings/errors após a deleção
- [x] Nenhuma outra tela quebrou (sem imports pendentes)

## Final Verification

- [x] Todos os acceptance criteria da spec atendidos
- [x] `flutter analyze` limpo
- [ ] Fluxo da InvestimentosPage testado manualmente (mobile ou web)
- [x] Nenhuma funcionalidade existente quebrada

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
