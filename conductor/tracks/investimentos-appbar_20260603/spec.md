# Specification: Refactor — InvestimentosPage para SharedAppBar

**Track ID:** investimentos-appbar_20260603
**Type:** Refactor
**Created:** 2026-06-03
**Status:** Draft

## Summary

Substituir o uso de `OrcamentosAppBar` pelo `SharedAppBar` padronizado
na `InvestimentosPage` e remover o arquivo `OrcamentosAppBar`, que ficará
sem uso após a migração.

## Context

O app possui o componente `SharedAppBar` em
`lib/features/shared/components/shared_appbar.dart`, já utilizado por
outras telas (orcamentos, configurações, notificações, etc.). A
`InvestimentosPage` é o único consumidor restante do `OrcamentosAppBar`
legado (`lib/components/common/orcamentos_appbar.dart`), que contém lógica
manual de avatar e tratamento web/mobile inline.

## User Story

N/A (refactor interno)

## Acceptance Criteria

- [ ] `InvestimentosPage` usa `SharedAppBar` no lugar de `OrcamentosAppBar`
- [ ] O método `_buildAppBar()` é removido da page
- [ ] O import de `OrcamentosAppBar` é removido da page
- [ ] O botão de refresh continua funcional via `actionButtons`
- [ ] A tela funciona normalmente em mobile e web (sem regressão visual ou funcional)
- [ ] O arquivo `lib/components/common/orcamentos_appbar.dart` é deletado
- [ ] `flutter analyze` passa sem warnings

## Dependencies

- `SharedAppBar` — `lib/features/shared/components/shared_appbar.dart`
- `InvestimentosPage` — `lib/components/investimentos_page/investimentos_page.dart`
- `OrcamentosAppBar` — `lib/components/common/orcamentos_appbar.dart` (único consumidor: InvestimentosPage)

## Out of Scope

- Alterações em outras pages além da `InvestimentosPage`
- Mudanças de comportamento ou lógica de negócio
- Ajustes visuais além da troca do componente de AppBar

## Technical Notes

O `SharedAppBar` exige: `title`, `subtitle`, `mainIcon`, `gradientColors`.
O botão de refresh deve ser passado via `actionButtons` usando
`SharedAppBar.headerButton(...)`. A lógica de avatar já está encapsulada
no `SharedAppBar` (parâmetro `showAvatar`), dispensando o código manual
de `CircleAvatar` + `Image.network` existente no `_buildAppBar()` atual.
`OrcamentosAppBar` pode ser deletado com segurança pois não há outros
consumidores.

---

_Generated by Conductor. Review and edit as needed._
