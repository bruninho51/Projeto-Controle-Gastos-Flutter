# Specification: Refactor — Notifications Feature

**Track ID:** notifications-refactor_20260603
**Type:** Refactor
**Created:** 2026-06-03
**Status:** Draft

## Summary

Migrar a feature de notificações de `lib/components/notificacoes_page/` e
`lib/features/notificacoes/` para `lib/features/notifications/`, renomear
classes e arquivos para inglês, extrair modelos e componentes em arquivos
próprios, e adotar SharedAppBar azul nas duas pages.

## Context

O app Orçamentos App possui uma feature de notificações bancárias que atualmente
reside em dois locais (components e features) com nomenclatura em português. A
estrutura está inconsistente com o padrão de organização por feature adotado em
refatorações recentes (dashboard, wave-animation).

## User Story

N/A — refatoração interna.

## Acceptance Criteria

- [ ] Todos os arquivos da feature residem em `lib/features/notifications/`
- [ ] Classes e arquivos renomeados para inglês (NotificationsChannel, NotificationsPage, NotificationEditPage)
- [ ] Modelos extraídos em arquivos próprios (`notification_model.dart`, `mapping_model.dart`)
- [ ] Componentes visuais extraídos em arquivos próprios em `components/`
- [ ] SharedAppBar azul adotado em `NotificationsPage` e `NotificationEditPage`
- [ ] Diretórios legados removidos (`lib/components/notificacoes_page/`, `lib/features/notificacoes/`)
- [ ] `flutter analyze` sem erros

## Dependencies

- `SharedAppBar` (existente em `lib/components/common/`)
- `lib/components/common/app_navigation_items.dart` (único consumidor externo)

## Out of Scope

- Alteração de qualquer regra de negócio ou comportamento visual
- Novos testes
- Alteração de outros módulos além de `app_navigation_items.dart`

## Technical Notes

A ordem de criação é crítica: modelos → channel → componentes → pages → cleanup.
Nenhum arquivo existente deve ser modificado antes de seu substituto estar pronto.
Substituir `.withOpacity()` por `.withValues(alpha:)` nos novos arquivos.

---

_Generated by Conductor. Review and edit as needed._
