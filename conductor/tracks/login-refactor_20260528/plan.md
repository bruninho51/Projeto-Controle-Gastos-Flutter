# Implementation Plan: Refactor — Extração de Componentes da Tela de Login

**Track ID:** login-refactor_20260528
**Spec:** [spec.md](./spec.md)
**Created:** 2026-05-28
**Status:** [~] In Progress

## Overview

Extrair `LoginBackgroundAnimation` (com `BackgroundWavePainter`), `LoginLogo` e `LoginCard`
em arquivos próprios sob `lib/features/auth/components/`, atualizando `login_page.dart`
para orquestrar os novos widgets. Cada fase extrai um componente isoladamente para
facilitar verificação incremental.

---

## Phase 1: Extração da Animação de Fundo

Criar `LoginBackgroundAnimation` encapsulando o `BackgroundWavePainter` e o
`AnimatedBuilder` que o utiliza. Os controladores de animação de onda permanecem
em `_LoginPageState` e são passados como parâmetros.

### Tasks

- [x] Task 1.1: Criar `lib/features/auth/components/login_background_animation.dart`
  com o widget `LoginBackgroundAnimation(w1, w2, w3)` e a classe `BackgroundWavePainter`
  (anteriormente `_BackgroundWavePainter`)
- [x] Task 1.2: Atualizar `login_page.dart` — substituir o bloco `Positioned.fill` +
  `AnimatedBuilder` + `CustomPaint` pelo novo `LoginBackgroundAnimation`, removendo
  a classe `_BackgroundWavePainter` do arquivo

### Verification

- [ ] `flutter analyze` sem warnings/errors
- [ ] A animação de ondas aparece e se move corretamente na tela de login

---

## Phase 2: Extração da Logo

Criar `LoginLogo` com a animação de escala elástica (`TweenAnimationBuilder`) e
o container branco com sombra que envolve o ícone do app.

### Tasks

- [ ] Task 2.1: Criar `lib/features/auth/components/login_logo.dart` com o
  widget `LoginLogo`, contendo o `TweenAnimationBuilder` e o `Container` com sombra
- [ ] Task 2.2: Atualizar `login_page.dart` — substituir a chamada `_buildLogo()`
  por `const LoginLogo()` e remover o método `_buildLogo`

### Verification

- [ ] `flutter analyze` sem warnings/errors
- [ ] A logo aparece com a animação de escala correta ao abrir a tela

---

## Phase 3: Extração do Card de Login

Criar `LoginCard` com todo o conteúdo do card inferior: título, subtítulo, botão
Google Sign-In (com estado de loading), divider e texto de termos. O componente
recebe `onSignIn` e `isLoading` como parâmetros, sem dependência direta do `AuthState`.

### Tasks

- [ ] Task 3.1: Criar `lib/features/auth/components/login_card.dart` com o widget
  `LoginCard({required VoidCallback onSignIn, required bool isLoading})`, contendo
  o `Container` de card, botão Google, divider e termos
- [ ] Task 3.2: Atualizar `login_page.dart` — substituir `_buildCard(context)` por
  `Consumer<AuthState>` que constrói `LoginCard(onSignIn: ..., isLoading: auth.isLoading)`;
  remover os métodos `_buildCard`, `_buildGoogleSignInButton` e `_buildDivider`

### Verification

- [ ] `flutter analyze` sem warnings/errors
- [ ] Botão Google Sign-In funciona e exibe o loading spinner durante autenticação
- [ ] Card renderiza com aparência idêntica ao estado anterior

---

## Final Verification

- [ ] Todos os critérios de aceitação da spec atendidos
- [ ] `lib/features/auth/components/` contém exatamente 3 arquivos: `login_background_animation.dart`, `login_logo.dart`, `login_card.dart`
- [ ] `login_page.dart` contém apenas: ciclo de vida, AnimationControllers, layout principal, carregamento de versão e métodos de texto simples (`_buildWelcomeText`, `_buildDescriptionText`, `_buildTermsText`, `_buildVersion`)
- [ ] `flutter analyze` limpo
- [ ] Comportamento visual idêntico verificado manualmente (animações, card, botão, logo)

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
